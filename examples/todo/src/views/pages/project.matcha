{> with project_name as String
{> with project_description as String
{> with todo_list as String
<section>
    <header>
        <h1>{{ project_name }}</h1>
        <p>{{ project_description }}</p>
    </header>
    {{ todo_list }}
    <footer>
        <a href="/">Back to All Todos</a>
    </footer>
</section>